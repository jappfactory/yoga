<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="windowNoTitle">true</item> <!-- 타이틀바 제거 -->
        <item name="android:windowFullscreen">true</item> <!-- 풀스크린 -->
        <item name="android:overScrollMode">never</item>
    </style>
    <style name="SplashTheme" parent="Theme.AppCompat.NoActionBar">
        <item name="android:windowBackground">@mipmap/splash</item>
    </style>
    <style name="MyAlertDialogStyle" parent="Theme.AppCompat.Light.Dialog">
        <item name="colorAccent">@color/DarkNavy2</item>
    </style>

    <style name="OfflineTheme" parent="Theme.AppCompat.NoActionBar">
        <item name="windowNoTitle">true</item> <!-- 타이틀바 제거 -->
        <item name="android:windowFullscreen">true</item> <!-- 풀스크린 -->
    </style>

    <style name="Toolbar" parent="AppTheme">
        <item name="android:autoCompleteTextViewStyle">@color/colorBlue</item>
        <!-- Toolbar Title Color -->
        <item name="android:textColorPrimary">@color/DarkNavy2</item>

        <!-- Toolbar Up Button Color -->
        <item name="android:textColorSecondary">@android:color/white</item>

    </style>
    <style name="Toolbar.TitleTextStyle" parent="@android:style/Widget.TextView">
        <item name="android:textSize">18sp</item>

    </style>

    <style name="MaterialSearchViewStyle">
        <!-- Background for the search bar-->

        <!-- Change voice icon-->
        <item name="searchVoiceIcon">@drawable/ic_action_voice_search_inverted</item>

        <!-- Change clear text icon-->
        <item name="searchCloseIcon">@drawable/ic_action_navigation_close_inverted</item>

        <!-- Change up icon-->
        <item name="searchBackIcon">@drawable/ic_action_navigation_arrow_back_inverted</item>

        <!-- Change icon for the suggestions-->
        <item name="searchSuggestionIcon">@drawable/ic_suggestion</item>

        <!-- Change background for the suggestions list view-->
        <item name="searchSuggestionBackground">@android:color/white</item>

        <!-- Change text color for edit text. This will also be the color of the cursor-->
        <item name="android:textColor">@color/colorBlueDark</item>

        <!-- Change hint text color for edit text-->
        <item name="android:textColorHint">@color/colorBlueDark</item>

        <!-- Hint for edit text-->
        <item name="android:hint">검색어를 입력하세요</item>
    </style>
</resources>
